---
- name: Extract backend into remote directory
    ansible.builtin.unarchive:
      src: files/artifact.tar.gz
      dest: ~/

- name: "Start app"
  become: yes
  shell:  |
    npm install
    pm2 stop default
    pm2 start npm -- start